//DIFERENCIAS ENTRE FUNCIONES Y MÉTODOS

const numero1 = 20;
const numero2 = "20";

console.log( parseInt(numero2) ); //parseInt es una función porque el parentesis va despues de la función.
console.log( numero1.toString() ); //.toString es un método porque lleva antes un punto y despues el paréntesis.